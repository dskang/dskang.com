<% wrap_layout :layout do %>
<div class="content">
  <div class="entry-header">
    <h2><%= current_article.title %></h2>
    <span class="entry-date"><%= current_article.date.strftime('%B %e, %Y') %></span>
  </div>
  <%= yield %>
</div>
<% end %>
