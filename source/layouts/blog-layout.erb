<% wrap_layout :layout do %>
<div class="content">
  <div class="entry-header">
    <h2><%= current_page.data.title %></h2>
    <span class="entry-date"><%= current_page.data.date.strftime('%B %e, %Y') %></span>
  </div>
  <%= yield %>
</div>
<% end %>
